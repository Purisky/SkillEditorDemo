using TreeNode.Runtime;
using TreeNode.Utility;
using UnityEngine;

namespace SkillEditorDemo.Model
{
    public enum TimeType
    {
        [LabelInfo("秒")]
        Second,
        [LabelInfo("帧")]
        GameTick,
    }
}
